CREATE TABLE `Customer_Satisfaction` (
                                         `id` int PRIMARY KEY AUTO_INCREMENT,
                                         `user_id` int,
                                         `type` int,
                                         `content` text
);